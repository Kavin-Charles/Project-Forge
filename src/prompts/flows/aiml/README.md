# AI / ML Setup Flow
This project type is planned but not yet implemented in `forge-gen` v1.0. Future updates will introduce dynamic prompts for AI frameworks, model endpoints, and Python ML pipelines.
